stream
  |from()
    .database('cre-freshdesk')
    .measurement('cre_freshdesk_5xx')
  |where(lambda: "ctlr" == 'Ember::Solutions::ArticlesController' AND "act" == 'folder_articles' AND "a_id" == '986130')
  |sum('count')
    .as('sum')
  |cumulativeSum('sum')
    .as('cumulative_sum')
  |derivative('cumulative_sum')
    .unit(1m)
    .nonNegative()
    .as('diff_1m')
  |window()
    .period(5m)
    .every(5m)
  |sum('diff_1m')
    .as('diff_tot')
  |alert()
      .message('[ {{ .Time }} ] [ Multichoice: Article Retrieve APIs has thrown 5xx error atleast once in last 5 minutes. ] [freshdesk_multichoice_kbase_article_retrieve_page_availability] {{ .Level }}')
      .crit(lambda: "diff_tot" >= 1)
      .critReset(lambda: "diff_tot" <= 1)
      .stateChangesOnly()
      .topic('cre-freshdesk')