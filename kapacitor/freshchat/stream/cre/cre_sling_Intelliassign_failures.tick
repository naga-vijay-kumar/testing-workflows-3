stream
  |from()
    .database('freshchat')
    .measurement('freshchat_cre_errors')
  |where(lambda: "appId" == '113893843993852' AND "type" == 'Agent' AND "method" =='IntelliAssign')
  |groupBy('type')
  |window()
    .period(10s)
    .every(10s)
  |mean('count')
    .as('IntelliAssign_assignment_failures')
  |alert()
    .message('[ {{ .Time }} ] [ {{ index .Tags "region" }} ] [ cre_sling_intelliassign_assignment_failures ] - {{ index .Fields "IntelliAssign_assignment_failures" }} {{ .Level }}')
    .id('{{ .Name }}:{{ .Group }}/cre_sling_intelliassign_assignment_failures')
    .crit(lambda: "IntelliAssign_assignment_failures" >= 5)
    .critReset(lambda: "IntelliAssign_assignment_failures" == 0)
    .stateChangesOnly()
    .topic('freshchat')
